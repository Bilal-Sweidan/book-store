{
  "name": "book-store",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client & npm run dev",
    "server": "cd server & npm run start",
    "react-build": "cd client & npm run build"
  },
  "author": "Bilal Sweidan",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "react-cookie": "^7.2.0"
  }
}
